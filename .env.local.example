# ! ==============================================================================
# ! CSE Smart Knowledge Base - Environment Variables
# ! ==============================================================================
# ! Copy this file to .env.local and fill in your values
# ! NEVER commit .env.local to git!
# ! ==============================================================================

# ------------------------------------------------------------------------------
# CONVEX (Backend-as-a-Service) - Set on .env.local
# ------------------------------------------------------------------------------
# Get from: https://dashboard.convex.dev
# Run `npx convex dev` to get these values automatically

# Frontend: Convex deployment URL
CONVEX_DEPLOYMENT=dev:
PUBLIC_CONVEX_URL=https://your-project.convex.cloud



# ! ------------------------------------------------------------------------------
# ! CONVEX DASHBOARD ENVIRONMENT VARIABLES
# ! ------------------------------------------------------------------------------
# ! Set these in Convex Dashboard > Settings > Environment Variables
# ! https://dashboard.convex.dev/t/username/yourproject/urlproject/settings/environment-variables
# ! ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# LLM API
# ------------------------------------------------------------------------------
# Provider: OpenAI-compatible API endpoint
# Endpoint: custom via LLM_API_URL

LLM_API_KEY=
LLM_API_URL=
LLM_MODEL=

EMBEDDING_API_KEY=
EMBEDDING_API_URL=
EMBEDDING_MODEL=

# ------------------------------------------------------------------------------
# GitHub API Token (for rate limit)
# ------------------------------------------------------------------------------
# Get from: https://github.com/settings/tokens
# Required scopes: public_repo (or repo for private repositories)
# Set in Convex Dashboard > Settings > Environment Variables

GITHUB_TOKEN=
